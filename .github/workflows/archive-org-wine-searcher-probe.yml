name: Probe Wayback Wine-Searcher merchant inventory

on:
  push:
    branches:
      - poc/wine-searcher-1000-20260813
    paths:
      - "poc/archive_org_wine_searcher_probe.py"
      - ".github/workflows/archive-org-wine-searcher-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --disable-pip-version-check requests
      - run: python poc/archive_org_wine_searcher_probe.py | tee archive-probe.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: archive-org-wine-searcher-probe
          path: archive-probe.log
          retention-days: 7
